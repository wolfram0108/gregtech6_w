# Every text file checks out with LF on every platform: file bytes reach the jar, and javac 17
# (the 1.20.1 toolchain) embeds source character offsets into synthetic names.
* text=auto eol=lf

# The Windows Gradle wrapper keeps CRLF for cmd.exe; it is never packed into the jar.
gradlew.bat text eol=crlf
