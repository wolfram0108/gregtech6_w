# This is the mods.toml of the 1.20.1 branch (Forge). It contains the data relating to the loading mods.
# There are several mandatory fields (#mandatory), and many more that are optional (#optional).
# The overall format is standard TOML format, v0.5.0.
# Note that there are a couple of TOML lists in this file.
# Find more information on toml format here:  https://github.com/toml-lang/toml

# Both loader fields are mandatory on Forge 1.20.1, unlike 26.x which defaulted them.
# A dedicated server fails to start without them.
modLoader="javafml" #mandatory
loaderVersion="[47,)" #mandatory

# The license for you mod. This is mandatory metadata and allows for easier comprehension of your redistributive properties.
# Review your options at https://choosealicense.com/. All rights reserved is the default copyright stance, and is thus the default here.
license="${mod_license}"

# A URL to refer people to when problems occur with this mod
issueTrackerURL="https://github.com/wolfram0108/gregtech6_w/issues"

# A list of mods - how many allowed here is determined by the individual mod loader
[[mods]] #mandatory

# The modid of the mod
modId="${mod_id}" #mandatory

# The version number of the mod
version="${mod_version}" #mandatory

# A display name for the mod
displayName="${mod_name}" #mandatory

# A URL to query for updates for this mod. See the JSON update specification https://docs.neoforged.net/docs/misc/updatechecker/
#updateJSONURL="https://change.me.example.invalid/updates.json" #optional

# A URL for the "homepage" for this mod, displayed in the mod UI
displayURL="https://github.com/wolfram0108/gregtech6_w"

# A file name (in the root of the mod JAR) containing a logo for display
#logoFile="examplemod.png" #optional



# The description text for the mod (multi line!) (#mandatory)
# Required attribution for a derivative work under the original mod's license.
authors="Gregorius Techneticies (GregTech 6); NeoForge port by wolfram0108"
credits="Ported from GregTech 6 for Minecraft 1.7.10 by Gregorius Techneticies."

description='''
GregTech 6 ported to Minecraft 1.20.1 / Forge.

An industrial process mod: ores are materials with composition, melting points and by-products,
processed through chains of machines from a hand hammer up to multiblock reactors and pressurised
fluid logistics. Items, blocks, ore dictionary entries and recipes are generated procedurally from a
material x prefix matrix rather than declared one by one.

UNOFFICIAL PORT: a derivative work under LGPL-3.0-or-later, not affiliated with or endorsed by
Gregorius Techneticies or the GregTech 6 team. Report problems to this port, not to them.
Provenance of every shipped component is in NOTICE, inside this jar.

ALPHA: playable end to end, but not finished. Worlds are not guaranteed to survive updates.
'''

# GT6 is really three of Gregorius's own separate mods bundled in one jar.
# The loader flags any @Mod without a matching entry here as dangling, so each needs its own block.
[[mods]] #mandatory
modId="gregapi" #mandatory
version="${mod_version}" #mandatory
displayName="Greg-API" #mandatory
description='''
Core API of GregTech 6. Part of the GregTech 6 distribution, not a separate mod.
'''

[[mods]] #mandatory
modId="gregapi_post" #mandatory
version="${mod_version}" #mandatory
displayName="Greg-API-Post" #mandatory
description='''
Late-loading half of the GregTech 6 core API. Part of the GregTech 6 distribution, not a separate mod.
'''

# The content mod is now a real neo @Mod, so its lifecycle actually runs.
# It loads after the other two, matching the original's own dependency order.
[[mods]] #mandatory
modId="gregtech" #mandatory
version="${mod_version}" #mandatory
displayName="GregTech" #mandatory
description='''
Content of GregTech 6: materials, machines, worldgen, recipes. Part of the GregTech 6 distribution.
'''

# @Mod.depends() on the Java side doesn't actually order loading - it only checks a dependency is present at all.
# Real load order comes from these ordering=BEFORE/AFTER sections instead, reproducing the original's link on both sides.
[[dependencies.gregapi]]
    modId="gregapi_post"
    mandatory=true
    versionRange="[${mod_version},)"
    ordering="BEFORE"
    side="BOTH"

[[dependencies.gregapi_post]]
    modId="gregapi"
    mandatory=true
    versionRange="[${mod_version},)"
    ordering="AFTER"
    side="BOTH"

# This mod loads after the other one, per the ordering direction declared here.
[[dependencies.gregtech]]
    modId="gregapi_post"
    mandatory=true
    versionRange="[${mod_version},)"
    ordering="AFTER"
    side="BOTH"

# The mod's one and only insertion into engine code, replacing the original's coremod transformers.
# Restores crafting-grid item position that 26.1.2 otherwise discards early.
[[mixins]]
config="gregapi.mixins.json"

# The [[accessTransformers]] block allows you to declare where your AT file is.
# If this block is omitted, a fallback attempt will be made to load an AT from META-INF/accesstransformer.cfg
#[[accessTransformers]]
#file="META-INF/accesstransformer.cfg"

# The coremods config file path is not configurable and is always loaded from META-INF/coremods.json

# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.${mod_id}]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # The type of the dependency. Can be one of "required", "optional", "incompatible" or "discouraged" (case insensitive).
    # 'required' requires the mod to exist, 'optional' does not
    # 'incompatible' will prevent the game from loading when the mod exists, and 'discouraged' will show a warning
    mandatory=true # mandatory is the Forge 1.20.1 spelling; type="required" is a later neo spelling.
    # Optional field describing why the dependency is required or why it is incompatible
    # reason="..."
    # The version range of the dependency
    versionRange="${neoforge_version_range}" #mandatory
    # An ordering relationship for the dependency.
    # BEFORE - This mod is loaded BEFORE the dependency
    # AFTER - This mod is loaded AFTER the dependency
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT, or SERVER
    side="BOTH"

# Here's another dependency
[[dependencies.${mod_id}]]
    modId="minecraft"
    mandatory=true
    # This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="BOTH"

# Features are specific properties of the game environment, that you may want to declare you require. This example declares
# that your mod requires GL version 3.2 or higher. Other features will be added. They are side aware so declaring this won't
# stop your mod loading on the server for example.
#[features.${mod_id}]
#openGLVersion="[3.2,)"
