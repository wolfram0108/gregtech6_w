package thaumcraft.common.container;

import net.minecraft.world.entity.player.Inventory;
import thaumcraft.common.tiles.TileArcaneWorkbench;

/** F10 ЗЕРКАЛО (compile-only) чужого API Thaumcraft. Только базовый класс для @Deprecated
 *  ContainerArcaneWorkbenchFixed (путь не исполняется без реального TC). См. compat-mirror/README.md. */
public class ContainerArcaneWorkbench {
	public ContainerArcaneWorkbench(Inventory aInventoryPlayer, TileArcaneWorkbench aTileEntity) {}
}
