package li.cil.oc.api.network;

/** F10 ЗЕРКАЛО (compile-only) чужого API — OpenComputers. Константа Network используется
 *  EnvironmentOC (Network.newNode(this, Visibility.Network)). См. compat-mirror/README.md. */
public enum Visibility {
	None, Neighbors, Network
}
