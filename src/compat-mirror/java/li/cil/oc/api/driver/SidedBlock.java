package li.cil.oc.api.driver;

/** F10 ЗЕРКАЛО (compile-only) чужого API — OpenComputers. Минимум для компиляции ядра; члены
 *  добираются компилятором (CompatOC.worksWith/createEnvironment не @Override — маркер).
 *  См. compat-mirror/README.md. */
public interface SidedBlock {}
