package cpw.mods.fml.client.registry;

/** F10 ЗЕРКАЛО (compile-only) — legacy Forge/FML 1.7.10 (пакет cpw.mods.fml не существует
 *  на neo-classpath). Только используемое GregTech6 (MultiTileEntityChest/MultiTileEntityMassStorage:
 *  bindTileEntitySpecialRenderer). См. compat-mirror/README.md. */
public class ClientRegistry {
	public static void bindTileEntitySpecialRenderer(Class<?> aTileEntityClass, Object aRenderer) {}
}
